/// Central route names for navigation (MaterialApp still uses widget home;
/// expand here when moving to named routes / GetMaterialApp).
abstract final class AppRoutes {
  static const String splash = '/';
  static const String login = '/login';
  static const String shell = '/shell';
}
